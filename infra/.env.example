# AWS Configuration (optional - uses AWS CLI profile if not set)
# AWS_ACCESS_KEY_ID=your_access_key
# AWS_SECRET_ACCESS_KEY=your_secret_key
# AWS_REGION=us-east-1

# LiveSpeech API Key (required for EC2 deployment)
LIVESPEECH_API_KEY=your_livespeech_api_key_here

# CDK will automatically detect these from AWS CLI:
# CDK_DEFAULT_ACCOUNT - Your AWS account ID
# CDK_DEFAULT_REGION - Your default region

# Usage:
# 1. Copy this file to .env: cp .env.example .env
# 2. Fill in your LIVESPEECH_API_KEY
# 3. Run: source .env && cdk deploy